export type TimeMode = "openTime" | "closeTime" | "openCloseTime";

export const TIME_MODE_LABEL: Record<TimeMode, string> = {
  openCloseTime: "Open-Close Time",
  openTime: "Open Time",
  closeTime: "Close Time",
};
